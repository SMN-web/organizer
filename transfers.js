export function showTransfersPanel(container, user) {
  // Insert your full transfer panel logic here.
  // Example: (Replace with real content as you develop)
  container.innerHTML = `<div class="transfers-panel-root"></div>`;
  // Optionally, call section initializers/components as needed.
}
